{
  "name": "@esp/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "export:evaluation": "tsx src/export-evaluation.ts ../../artifacts/evaluation-runs/app-candidate-results.json"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "express": "5.2.1",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@types/express": "5.0.3",
    "tsx": "4.20.5",
    "vitest": "3.2.4"
  }
}
